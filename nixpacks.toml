[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "sqlite", "openssl"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.start]
cmd = "python -m app.main"
